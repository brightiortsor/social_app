import { createTheme } from "@mui/system";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#1760a5",
      light: "#4f8cd8",
    },
    secondary: {
      main: "#f9a825",
      light: "#fcd46d",
    },
    otherColor: {
      main: "#999",
    },
  },
});
